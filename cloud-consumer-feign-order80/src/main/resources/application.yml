server:
  port: 80

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka

# 默认一秒钟，单位毫秒
ribbon:
  ReadTimeout : 5000
  ConnecTimeout : 5000


logging:
  level:
    com.xy.springcloud.service.PaymentFeignService: debug
